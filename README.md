# Date Adder

A simple Python utility to automate renaming files by prepending today's date in `YYYY-MM-DD` format to the beginning of every filename in a specified folder.

## Setup & Usage

1. Put the files you want to rename in a folder (e.g., `test_files`).
2. Run the script:
   ```bash
   python add_date.py
   ```
